#!/usr/bin/env bash #shebang that says bash...what
set -euo pipefail #
IFS=$'\n\t'       #
set -vx           #

bundle install    # bundler is installed 

# Do any other automated setup that you need to do here
